# Test-only dependencies. Install with:  pip install -r requirements-test.txt
# The app's own runtime deps (fastapi/starlette, torch, bcrypt, soundfile, ...) come
# from the main requirements; these are what the test suite adds on top.
pytest>=7.4.0
httpx>=0.24.0          # used by starlette's TestClient
